coverage:
  status:
    project:
      default: false
      main:
        paths:
          - "src/seals/"
          - "!src/seals/testing/"
      tests:
        # Should not have dead code in our tests
        target: 100%
        paths:
          - "tests/"
          - "src/seals/testing/"
    patch:
      default: false
      main:
        paths:
          - "src/seals/"
          - "!src/seals/testing/"
      tests:
        target: 100%
        paths:
          - "tests/"
          - "src/seals/testing/"
